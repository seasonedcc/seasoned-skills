A task is not done if it adds or changes a user-facing product surface without shipping its dev-seed section and its entry in the committed seed manifest ({{seed-manifest}}) in the same change. Every surface either seeds its demo-critical state or is declared unseedable there with an honest written reason — never silently omitted. {{seed-denominator}} Keep the dev seed single-shot and fail-loud behind its empty-database preflight: never reseed a populated database and never add a reset script — to reseed, drop and recreate the database.
