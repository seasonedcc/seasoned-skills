## Working with /goal goals

A `/goal` goal follows the same orchestration approach as everything else. The one difference: a goal allows multiple PRs to be merged during development, against the goal's base branch — the default branch unless the goal says otherwise. As you personally review each PR, feel free to merge it to the base branch when you consider it ready. The only rule is not to merge broken work.
