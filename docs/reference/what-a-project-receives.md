# What a project receives

What you commit is your configuration and your own writing. Almost everything
else the workflow needs inside the project is generated by
`seasoned-skills sync`, gitignored, and rewritten from scratch on every run.
That is what makes an upgrade a version bump rather than a merge. Two things it
needs are not generated: the demo narrator's model weights, which the demo-video
rig's own setup fetches, and your per-user meeting settings.
[The managed footprint](managed-footprint.md) keeps both out of your history.

## What you commit

| File | Who owns it |
| --- | --- |
| `seasoned-skills.config.ts` | You. Every option, stated explicitly. |
| Your content directory | You. One markdown file per generated skill, plus one for the standing instructions. All optional. |
| The calibration file | You. How this project's subagents are calibrated against the Definition of Done. |
| The registers your options need | You. The coverage register, excused pages, seed manifest, parity standard, exception register. |
| `shaping/` | You. Your shaping documents. Its `assets/` folder is generated. |
| `requests-from-meetings/` | You. The records parsed from meetings. Its `assets/style.css` is generated. |
| `.claude/settings.json` | You, with a few keys the package keeps true. |
| `.gitignore` | You, with one managed block. |
| `package.json` | You, with one managed script entry. |

The last three are the whole managed footprint, and
[its own page](managed-footprint.md) covers exactly what gets touched.

## What a sync generates

| File | What it is |
| --- | --- |
| `CLAUDE.md` | The standing instructions every session reads first, woven from your own content and the workflow's rules, Definition of Done included. |
| `.claude/skills/<name>/` | One folder per enabled skill, holding its `SKILL.md` and any reference files it ships. |
| `.claude/skills/shaping/references/` | The shaping reference library, woven in from this machine's cache. Absent until that cache exists: the install builds it, and `seasoned-skills corpus` rebuilds it. |
| `.claude/skills/subagents/scripts/watchdog.py` | Watches a subagent's context and raises the alarm before it fills. |
| `.claude/skills/requests-from-meetings/scripts/verify.py` | Verifies a parsed meeting record against its transcript. |
| `.claude/skills/demo-videos/scripts/` | The rig that films and narrates demo videos. |
| `.claude/hooks/` | Three hooks: one blocks `git stash`, one guards worktree isolation, one sweeps lane processes when a session ends. |
| `.claude/statusline.sh` | The status line, including the context bar you watch during a session. |
| `.claude/seasoned-skills-manifest.json` | The list of every generated path, which is how the next sync knows what to delete. |
| `shaping/assets/` | The stylesheet, script, drawing tools, and page template that shaping documents load. |
| `requests-from-meetings/assets/style.css` | The stylesheet the committed meeting records load. |

None of it enters your history, and none of it is yours to edit. A change you
want to keep goes in your content directory or in your configuration, and the
next sync weaves it in.

## The skills

Every project receives the core set. The rest arrive with the options that turn
them on, and disappear again when the option goes away.

### Every project

| Skill | What it is for |
| --- | --- |
| `orchestration` | Delegating work: writing a subagent's instructions, checking its claims, recovering after an interruption, shipping a lane's pull request. |
| `subagents` | Sizing each subagent's task to the context window, and picking the right model for it. |
| `worktrees` | Running parallel work in isolated lanes, provisioned through this package's binary. |
| `prepare-for-compaction` | Making the session's ledger current so its context can be compacted safely. You type this one, never the agent. |
| `reground` | Coming back after a compaction through the ledger and the real sources, never the summary alone. |
| `testing` | Writing, running, and debugging both suites, including mutation proofs and flaky-test diagnosis. |
| `quick` | The reduced Definition of Done for a small polish. Only your own `/quick` turns it on. |
| `pr-review` | Reviewing a pull request with its full history, then finding and verifying issues adversarially. |
| `post-review` | Posting a finished review on someone's pull request as inline comments with a verdict. |
| `review-fixes` | Turning a review's findings into a decided fix scope and landing them on the reviewed branch. |
| `main-sync` | Bringing the default branch's new commits into a long-lived feature branch. |
| `release` | Cutting a release: scoping the span, studying every merged pull request in it, writing notes that trace to real changes. |
| `self-improvement` | Turning a finished task's lessons into a pull request on your project and an issue on this package. |
| `shaping` | Running framing and shaping sessions that end in a shaping document, taught from the reference library. |
| `requests-from-meetings` | Turning meeting recordings into precise feature-request documents. |
| `demo-videos` | Filming two narrated demos of a scope of work: a highlights cut and a full walkthrough. |
| `skill-management` | Creating, structuring, and debugging Claude Code skills. |
| `seasoned-skills` | Installing, upgrading, configuring, and repairing this package inside your project. |

### With `webSurface`

| Skill | What it is for |
| --- | --- |
| `design-system` | Following your project's design system when building or changing pages. |
| `agent-browser` | Driving a browser from the command line, and keeping its processes from leaking. |

### With `stack`

| Skill | What it is for |
| --- | --- |
| `authorization` | Authorization through context getters, context schemas, and composable functions. |
| `business-folder` | Organizing business logic by domain, independent of the framework. |
| `composable-functions` | Type-safe business logic with the composable-functions library. |
| `database-design` | Tables and migrations under the identity-and-event-table rules. |
| `kysely` | Queries and migrations that follow the project's conventions. |
| `background-jobs` | Background and cron jobs on graphile-worker. |
| `framework-folder` | Keeping framework code and application code apart. |
| `env-vars` | Environment variables under the two-tier pattern. |
| `nested-routes` | Nested route sections with layouts, index redirects, and menu navigation. |
| `optimistic-ui` | Optimistic updates built on deterministic resource routes. |
| `formatting-datetimes` | Formatting dates in SQL instead of in JavaScript `Date` instances. |
| `type-safety` | Minimal, correct TypeScript annotations. |

### With `stack` and `machineSurface`

| Skill | What it is for |
| --- | --- |
| `mcp-server` | Building and extending the MCP server, held to parity with the product. |

## Code you can import

Two rules need a real check behind them, not a promise to look: the
identifier-length rule the `kysely` skill teaches, and the demo-seed criterion
in your Definition of Done. The package exports the checking parts so your own
test suite can run them, and the generated skills teach the rest.

The first three guard Postgres identifier length. Postgres silently truncates a
name past 63 bytes, and the stack's camel-case plugin expands compiled index and
constraint names, so a declared name can land in the database truncated and stay
wrong until something references it.

| Export | What it is |
| --- | --- |
| `IDENTIFIER_BYTE_LIMIT` | 63, the length Postgres truncates at. |
| `IDENTIFIER_LENGTH_AUDIT_SQL` | The query returning every identifier in your schema that sits at the limit. Run it with any query runner against a migrated database. |
| `identifierLengthFailures` | Turns those rows into one message per offender, or an empty list. |

The rest are the demo-seed criterion's checker. It derives the list of pages
from your own route config rather than from a list someone maintains by hand, so
a page nobody covered fails the gate instead of depending on whether a reviewer
noticed.

| Export | What it is |
| --- | --- |
| `routeId` | A route's id from its module path, the same derivation the coverage gates use. |
| `enumerateSurfaces` | The pages of a route config. An entry that declares children is a layout, not a page, so only childless entries count. |
| `seedCoverage` | Compares your route config against the manifest's claims and reports what is unclaimed, what claims a page that no longer exists, and which exclusions match nothing. |
| `seedCoverageFailures` | Turns that report into one message per failure, each naming the fix. |

The types come with them: `IdentifierRow`, `RouteConfigEntry`,
`SeedCoverageInput`, and `SeedCoverageReport`, plus `SeasonedSkillsConfig` for
the configuration itself.

## Where to go next

[Configuration](configuration.md) covers the keys that decide which of this you
get. [The managed footprint](managed-footprint.md) covers the few files sync
touches that are otherwise yours.
