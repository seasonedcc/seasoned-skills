---
"seasoned-skills": patch
---

Keep a goal's state on a Vertical board. The orchestrator seeds `build.vertical` beside the shaping document, is its only writer, and regrounds from its summary; the person watches it read-only or adds requests through its inbox. Doctor now looks for `itsvertical`.
